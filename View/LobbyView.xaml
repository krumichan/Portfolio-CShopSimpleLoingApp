<UserControl x:Class="LoginApp.View.LobbyView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LoginApp.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Vertical" Margin="0, 10">
        
        <!-- 현재 접속해 있는 User 이름 -->
        <Label Content="{Binding CurrentUserName}" />
        <Label Content="Welcome to my world!" />
        
        <!-- 각종 이벤트 관련 구간 -->
        <StackPanel Orientation="Horizontal" Margin="10, 10">
            <!-- 로그아웃 이벤트 -->
            <Button Command="{Binding LogoutCommand}" Width="100" HorizontalAlignment="Left" Margin="10, 0">Logout</Button>
            <!-- 회원수정화면 호출 이벤트 -->
            <Button Command="{Binding ModifyAccountCommand}" Width="100" HorizontalAlignment="Left" Margin="10, 0">Modify Account</Button>
            <!-- 회원삭제 이벤트 -->
            <Button Command="{Binding DeleteAccountCommand}" Width="100" HorizontalAlignment="Left" Margin="10, 0">Delete Account</Button>
        </StackPanel>
        
    </StackPanel>
</UserControl>
